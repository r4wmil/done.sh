_done() {
	local cur
	cur="${COMP_WORDS[COMP_CWORD]}"
	if [ -f "{donefile}" ]; then
		COMP=`cat "{donefile}" | awk -F '%COMPLETIONS=' '{print $2}'`
	fi
	COMPREPLY=($(compgen -W "$COMP" -- "$cur"))
}
complete -F _done done.sh
